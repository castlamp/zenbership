$(document).ready(function () {    var timeSec = $('#time_sec_text');    var timeMin = $('#time_min_text');    var timer = 0;    var second = 0;    $('#timer_button').bind('click', function (e) {        e.preventDefault();        if ($(this).val() == 'Start') {            $('#time_sec_text').show();            timer = setInterval(runStopwatch, 1000);            $(this).val('Stop');        } else {            clearInterval(timer);            $(this).val('Start');        }    });    function runStopwatch() {        second++;        var minute = parseFloat($('#time_min_text').val());        if (second > 59) {            second = 0;            minute = minute + 1;        }        if (inInt(minute) == '1' && minute != '0' && second == 0) {            play_sound('misc/sounds/invoice_timer.wav');        }        timeMin.val("0".substring(minute >= 10) + minute);        timeSec.html("0".substring(second >= 10) + second);    }    function inInt(x) {        if (x % 15 == 0) {            return '1';        } else {            return '0';        }    }});