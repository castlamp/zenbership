$("input[name='product[type]']").live("change", function () {    $('.ptype').not('#' + display).hide();    var display = 'sub_' + $(this).val();    if ($(this).val() == '2') {        show_div('sub_2');    }    else if ($(this).val() == '3') {        show_div('sub_2');        show_div('sub_3');    }    else {        show_div(display);    }});$("input[name='product[physical]']").live("change", function () {    if ($(this).val() == '1') {        show_div('physical');    } else {        hide_div('physical');    }});function submit_product(id, edit) {    var check = check_form();    if (check === true) {        // add('product',id,edit);        json_add('product-add', id, edit, 'popupform');    }    else {        handle_error('Please check the form for errors.');    }    return false;}function addtier(id) {    tiers += 1;    show_loading();    send_data = 'action=tier&id=' + id + '&number=' + tiers;    $.post('cp-functions/product_addition.php', send_data, function (repSo) {        $('#tier_options tbody').append(repSo);        close_loading();    });    return false;}function add_reminder(id) {    reminders += 1;    show_loading();    send_data = 'action=reminder&id=' + id + '&number=' + reminders;    $.post('cp-functions/event_reminders.php', send_data, function (repSo) {        $('#event_reminder_new').append(repSo);        close_loading();    });    return false;}function delete_tier(id) {    $('#tier_opt-' + id).remove();}function addoption(id) {    option += 1;    show_loading();    send_data = 'action=option&id=' + id + '&number=' + option;    $.post('cp-functions/product_addition.php', send_data, function (repSo) {        $('#stock_options').append(repSo);        close_loading();    });    return false;}function delete_option(id) {    $('#opt-' + id).remove();}function add_inner_option(anOption, id) {    inner_option += 1;    show_loading();    send_data = 'action=inner_option&id=' + id + '&main_option=' + anOption + '&number=' + inner_option;    $.post('cp-functions/product_addition.php', send_data, function (repSo) {        $('#opt-table-' + anOption + ' tbody').append(repSo);        close_loading();    });    return false;    //$('#opt-table-' + anOption + ' tbody').append(row);}function delete_inner_option(anOption, id) {    $('#inner_option-' + anOption + '-' + id).remove();}function add_upsell(id, type) {    show_loading();    send_data = 'action=upsell_option&id=' + id + '&when=' + type;    $.post('cp-functions/product_addition.php', send_data, function (repSo) {        $('#upsell_options tbody').append(repSo);        close_loading();    });    return false;}function delete_upsell(id) {    $('#upsell_opt-' + id).remove();}